<div class="pop-up-emergency d-flex">
  <div class="alinhar">
    <h1>Voce tem uma nova emergencia!</h1>
    <button>OK</button>
  </div>
</div>


<div class="accordion" id="accordionPanelsStayOpenExample" data-controller="geolocation">
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseOne" aria-expanded="true" aria-controls="panelsStayOpen-collapseOne">
        Informações sobre a occorência:
      </button>
    </h2>
    <div id="panelsStayOpen-collapseOne" class="accordion-collapse collapse ">
      <div class="accordion-body">
        <p class="fw-bold">Endereço: <%= @emergency.street %></p>
        <p class="fw-bold">Descrição: <%= @emergency.description %></p>
      </div>

    </div>
  </div>
</div>
<div class="centraliziation-of-the-map ">
  <div class="" id='map' style='width: 100%; height: 100%;'
    data-controller="map-show-emergency"
    data-map-show-emergency-emergencies-markers-value="<%= @emergencies_markers.to_json %>"
    data-map-show-emergency-schedules-markers-value="<%= @schedules_markers.to_json %>"
    data-map-show-emergency-emergency-marker-value="<%= @emergency_marker.to_json %>"
    data-map-show-emergency-lat-value="<%= @lat %>"
    data-map-show-emergency-long-value="<%= @long %>"
    data-map-show-emergency-slon-value="<%= @slon %>"
    data-map-show-emergency-slat-value="<%= @slat %>"
    data-map-show-emergency-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>" ></div>
</div>
