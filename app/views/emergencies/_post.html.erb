<div id="post-<%= post.id %>">
  <small>
    <strong>Central: <%= post.user.id %></strong>
    <i><%= post.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= post.content %></p>
</div>
